## Agent skills

### Issue tracker

작업은 Linear의 `Allganize` 워크스페이스, `AIN` 팀, `meeting` 프로젝트에서 관리합니다. 자세한 규칙은 `docs/agents/issue-tracker.md`를 참고합니다.

### Triage labels

기본 triage 역할과 같은 이름의 Linear 라벨을 사용합니다. 자세한 매핑은 `docs/agents/triage-labels.md`를 참고합니다.

### Domain docs

이 저장소는 루트 `CONTEXT.md`와 `docs/adr/`를 사용하는 단일 컨텍스트 구조입니다. 소비 규칙은 `docs/agents/domain.md`를 참고합니다.
